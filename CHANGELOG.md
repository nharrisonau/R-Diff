# Changelog

## 0.2.1 - In progress

- Documented the shift from `ground-truth` instrumentation to `prev-safe` baselines built from the
  previous release to support static diffing of backdoor introductions.
- Clarified the repository layout (`components/` vs. `firmware/`) and the expected variant structure
  (`original/`, `previous/`, and per-target Makefiles).
- Added documentation covering the expanded benchmark set, including Dropbear and the OpenWrt
  firmware image.

## 0.1.0 - 0.2.0

Initial version of the ROSARUM benchmark, as presented in the ICSE'25 paper.
